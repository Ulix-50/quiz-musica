import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.quiz.musica',
  appName: 'prjQuiz_Musica',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
